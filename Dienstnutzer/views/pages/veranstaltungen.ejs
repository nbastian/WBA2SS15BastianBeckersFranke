<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
</head>
<body class="container">

    <header>
        <% include ../partials/header %>
    </header>

    <main>
        <div class="row">
            <div class="col-sm-12">
                <ul class="list-group">
                    <% veranstaltungen.forEach(function(veranstaltungen) { %>
                    <a href="/" class="list-group-item active"><%= veranstaltungen.name %></a>
                    <% }); %>
                </ul>
            </div>
        </div>
    </main>

    <footer>
        <% include ../partials/footer %>
    </footer>
    
</body>
</html>